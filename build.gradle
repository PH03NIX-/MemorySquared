buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.0.0' // or the latest compatible version
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
